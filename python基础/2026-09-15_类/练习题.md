# Python 类 — 练习题

> 基于第9章（类）

---

## 一、选择题

### 1. 以下哪个是正确定义类的方式？~~A~~ B

A. `class MyClass():`  
B. `class MyClass:`  
C. `def MyClass:`  
D. `object MyClass:`

---

### 2. `__init__` 方法的作用是？B

A. 销毁对象  
B. 初始化对象  
C. 定义类变量  
D. 导入模块

---

### 3. 方法中的 `self` 代表什么？B

A. 类本身  
B. 当前实例对象  
C. 父类  
D. 全局变量

---

### 4. 以下代码的输出是什么？~~B~~ A

```python
class Dog:
    species = "犬科"
    
    def __init__(self, name):
        self.name = name

d1 = Dog("旺财")
d2 = Dog("来福")
d1.species = "猫科"
print(d2.species)
```

A. `犬科`  
B. `猫科`  
C. 报错  
D. `None`

---

### 5. 类变量和实例变量的区别是？A

A. 类变量所有实例共享，实例变量每个实例独有  
B. 类变量每个实例独有，实例变量所有实例共享  
C. 没有区别  
D. 类变量不能修改

---

### 6. 以下代码的输出是什么？B

```python
class Animal:
    def speak(self):
        return "..."

class Dog(Animal):
    def speak(self):
        return "汪汪！"

dog = Dog()
print(dog.speak())
```

A. `...`  
B. `汪汪！`  
C. 报错  
D. `None`

---

### 7. 调用父类方法使用？B

A. `parent.method()`  
B. `super().method()`  
C. `this.method()`  
D. `base.method()`

---

### 8. `isinstance(dog, Animal)` 检查的是？B

A. `dog` 是否是 `Animal` 类  
B. `dog` 是否是 `Animal` 或其子类的实例  
C. `Animal` 是否是 `dog` 的子类  
D. `dog` 和 `Animal` 是否相同

---

### 9. 以下哪种命名表示"私有"变量（名称改写）？B

A. `_name`  
B. `__name`  
C. `__name__`  
D. `name_`

---

### 10. 实现迭代器需要定义哪两个方法？B

A. `__init__` 和 `__next__`  
B. `__iter__` 和 `__next__`  
C. `__start__` 和 `__end__`  
D. `__begin__` 和 `__next__`

---

### 11. 生成器使用哪个关键字产出值？B

A. `return`  
B. `yield`  
C. `yield_all`  
D. `generate`

---

### 12. 以下代码的输出是什么？B

```python
def counter():
    n = 0
    while True:
        n += 1
        yield n

c = counter()
print(next(c))
print(next(c))
print(next(c))
```

A. `1 1 1`  
B. `1 2 3`  
C. `0 1 2`  
D. 报错

---

### 13. 列表推导式和生成器表达式的区别是？A

A. 列表用 `[]`，生成器用 `()`  
B. 列表用 `()`，生成器用 `[]`  
C. 没有区别  
D. 列表用 `{}`，生成器用 `()`

---

### 14. 以下代码的输出是什么？C

```python
class Dog:
    tricks = []
    
    def add_trick(self, trick):
        self.tricks.append(trick)

d1 = Dog()
d2 = Dog()
d1.add_trick("翻滚")
d2.add_trick("握手")
print(d1.tricks)
```

A. `['翻滚']`  
B. `['握手']`  
C. `['翻滚', '握手']`  
D. `[]`

---

### 15. `@dataclass` 装饰器的作用是？B

A. 定义抽象类  
B. 自动生成 `__init__` 等方法  
C. 定义私有类  
D. 定义静态方法

---

## 二、填空题

### 16. 定义类使用 ___class___ 关键字，初始化方法是 _____init_____。

---

### 17. 方法的第一个参数通常是 ___self___，代表___类实例本身___。

---

### 18. 类变量定义在类内部,，方法外______，实例变量定义在_____init__内部___。

---

### 19. 继承使用 `class Child(__parent____)`，调用父类方法用 ___super___().method()。

---

### 20. 检查对象类型的函数是 isinstance______，检查类继承关系的函数是 ___isSubClass___。

---

### 21. 迭代器需要实现 __iter________ 和 _____next_____ 方法。

---

### 22. 生成器使用 ___yield___ 关键字，生成器表达式用 ___()___ 括号。

---

### 23. 受保护变量以 __单下划线__ 开头，私有变量以 ___双下划线_____ 开头。

---

## 三、判断题

### 24. Python 有真正的私有变量，外部完全无法访问。（X ）

---

### 25. 类变量用 `self.xxx` 定义。（ X）

---

### 26. 子类可以重写父类的方法。（V ）

---

### 27. `super()` 只能调用父类的 `__init__` 方法。（ X）

---

### 28. 生成器函数用 `return` 产出值。（ X）

---

### 29. 生成器表达式比列表推导式更省内存。（ V）

---

### 30. 一个类只能继承一个父类。（X ）

---

### 31. `self` 是 Python 的关键字，不能用其他名字。（ V）X

---

## 四、代码阅读题

### 32. 写出输出：

```python
class Counter:
    count = 0
    
    def __init__(self):
        Counter.count += 1
    
    @classmethod
    def get_count(cls):
        return cls.count

c1 = Counter()
c2 = Counter()
c3 = Counter()
print(Counter.count) 3
print(c1.get_count())3
```

---

### 33. 写出输出：

```python
class Animal:
    def __init__(self, name):
        self.name = name
    
    def info(self):
        return f"动物：{self.name}"

class Dog(Animal):
    def __init__(self, name, breed):
        super().__init__(name)
        self.breed = breed
    
    def info(self):
        return f"狗：{self.name}，品种：{self.breed}"

dog = Dog("旺财", "柴犬")
print(dog.info())狗：旺财，品种：柴犬
print(isinstance(dog, Animal)) True
```

---

### 34. 写出输出：

```python
def fibonacci():
    a, b = 0, 1
    while True:
        yield a
        a, b = b, a + b

fib = fibonacci()
print(next(fib)) 0 1
print(next(fib)) 1 1
print(next(fib)) 1 2
print(next(fib)) 2 3
print(next(fib)) 3 5
```

---

### 35. 写出输出：

```python
class Stack:
    def __init__(self):
        self.items = []
    
    def push(self, item):
        self.items.append(item)
    
    def pop(self):
        return self.items.pop()
    
    def is_empty(self):
        return len(self.items) == 0

s = Stack()
s.push(1) 
s.push(2)
s.push(3)
print(s.pop()) 3
print(s.pop()) 2
print(s.is_empty()) False
```

---

### 36. 写出输出：

```python
squares = [x**2 for x in range(5)]
gen = (x**2 for x in range(5))

print(type(squares))<class 'list'>
print(type(gen)) <class 'generator'>
print(squares)[0,1,4,9,16]
print(list(gen)) 1 [0,1,4,9,16]
```

---

## 五、编程题

### 37. 定义类

定义一个 `Rectangle` 类：
1. 有 `width` 和 `height` 属性
2. 有 `area()` 方法返回面积
3. 有 `perimeter()` 方法返回周长
4. 创建两个矩形对象并计算面积

---

### 38. 继承

定义以下类：
1. `Shape` 基类，有 `area()` 和 `perimeter()` 方法（返回 None）
2. `Circle` 继承 `Shape`，有 `radius` 属性
3. `Rectangle` 继承 `Shape`，有 `width` 和 `height` 属性
4. 测试计算圆和矩形的面积

---

### 39. 类变量陷阱

修正以下代码，使每个 `Student` 有自己独立的课程列表：

```python
class Student:
    courses = [] (X)
    
    def __init__(self, name):
        self.courses = []
        self.name = name
    
    def add_course(self, course):
        self.courses.append(course)

s1 = Student("Alice")
s2 = Student("Bob")
s1.add_course("Math")
s2.add_course("English")
print(s1.courses)  # 应该是 ['Math']，但实际是 ['Math', 'English']
```

---

### 40. 迭代器

实现 `Range` 类，模拟 `range()` 的行为：
1. `__init__(start, stop)`
2. `__iter__()` 返回自身
3. `__next__()` 返回下一个值
4. 测试：`for i in Range(1, 5): print(i)`

---

### 41. 生成器

编写生成器函数 `even_numbers(n)`：
1. 产出 0 到 n 之间的所有偶数
2. 测试：`for num in even_numbers(10): print(num)`

---

### 42. 综合题：银行账户

实现 `BankAccount` 类：
1. `__init__(owner, balance=0)`：开户
2. `deposit(amount)`：存款
3. `withdraw(amount)`：取款（余额不足时抛异常）
4. `get_balance()`：查询余额
5. `__str__()`：返回账户信息

测试：
```python
acc = BankAccount("Alice", 1000)
acc.deposit(500)
acc.withdraw(200)
print(acc)  # Alice 的账户，余额：1300
```

---

### 43. 综合题：学生管理系统

实现以下类：
1. `Student`：有 `name`、`scores`（字典）属性
2. `add_score(subject, score)`：添加成绩
3. `get_average()`：计算平均分
4. `StudentManager`：管理多个学生
5. `add_student(student)`：添加学生
6. `get_top_student()`：找出最高平均分的学生

---

### 44. 综合题：链表迭代器

实现简单的链表：
1. `Node` 类：有 `value` 和 `next` 属性
2. `LinkedList` 类：有 `head` 属性
3. `append(value)`：添加节点
4. 实现 `__iter__()` 使其可以 for 循环遍历

测试：
```python
ll = LinkedList()
ll.append(1)
ll.append(2)
ll.append(3)
for val in ll:
    print(val)  # 1, 2, 3
```

---

## 参考答案

<details>
<summary>点击查看答案</summary>

### 选择题

1. B
2. B
3. B
4. A（d1.species 创建实例变量，不影响类变量）
5. A
6. B
7. B
8. B
9. B
10. B
11. B
12. B
13. A
14. C（类变量被共享）
15. B

### 填空题

16. `class`；`__init__`
17. `self`；当前实例对象
18. 类内部（方法外）；`__init__` 方法里
19. `Parent`；`super()`
20. `isinstance()`；`issubclass()`
21. `__iter__`；`__next__`
22. `yield`；圆
23. 单下划线 `_`；双下划线 `__`

### 判断题

24. ✗（可以访问 `_ClassName__name`）
25. ✗（类变量直接定义在类里，不用 self）
26. ✓
27. ✗（可以调用任何父类方法）
28. ✗（用 yield）
29. ✓
30. ✗（支持多重继承）
31. ✗（只是约定，可以用其他名字）

### 代码阅读题

**32.**
```
3
3
```

**33.**
```
狗：旺财，品种：柴犬
True
```

**34.**
```
0
1
1
2
3
```

**35.**
```
3
2
False
```

**36.**
```
<class 'list'>
<class 'generator'>
[0, 1, 4, 9, 16]
[0, 1, 4, 9, 16]
```

### 编程题

**37.**
```python
class Rectangle:
    def __init__(self, width, height):
        self.width = width
        self.height = height
    
    def area(self):
        return self.width * self.height
    
    def perimeter(self):
        return 2 * (self.width + self.height)

r1 = Rectangle(3, 4)
r2 = Rectangle(5, 6)
print(r1.area())        # 12
print(r2.perimeter())   # 22
```

**38.**
```python
import math

class Shape:
    def area(self):
        return None
    
    def perimeter(self):
        return None

class Circle(Shape):
    def __init__(self, radius):
        self.radius = radius
    
    def area(self):
        return math.pi * self.radius ** 2
    
    def perimeter(self):
        return 2 * math.pi * self.radius

class Rectangle(Shape):
    def __init__(self, width, height):
        self.width = width
        self.height = height
    
    def area(self):
        return self.width * self.height
    
    def perimeter(self):
        return 2 * (self.width + self.height)

c = Circle(5)
r = Rectangle(3, 4)
print(f"圆面积：{c.area():.2f}")
print(f"矩形面积：{r.area()}")
```

**39.**
```python
class Student:
    def __init__(self, name):
        self.name = name
        self.courses = []  # 改为实例变量
    
    def add_course(self, course):
        self.courses.append(course)

s1 = Student("Alice")
s2 = Student("Bob")
s1.add_course("Math")
s2.add_course("English")
print(s1.courses)  # ['Math']
print(s2.courses)  # ['English']
```

**40.**
```python
class Range:
    def __init__(self, start, stop):
        self.current = start
        self.stop = stop
    
    def __iter__(self):
        return self
    
    def __next__(self):
        if self.current >= self.stop:
            raise StopIteration
        value = self.current
        self.current += 1
        return value

for i in Range(1, 5):
    print(i)  # 1, 2, 3, 4
```

**41.**
```python
def even_numbers(n):
    for i in range(0, n + 1, 2):
        yield i

for num in even_numbers(10):
    print(num)  # 0, 2, 4, 6, 8, 10
```

**42.**
```python
class BankAccount:
    def __init__(self, owner, balance=0):
        self.owner = owner
        self.balance = balance
    
    def deposit(self, amount):
        if amount > 0:
            self.balance += amount
    
    def withdraw(self, amount):
        if amount > self.balance:
            raise ValueError("余额不足")
        self.balance -= amount
    
    def get_balance(self):
        return self.balance
    
    def __str__(self):
        return f"{self.owner} 的账户，余额：{self.balance}"

acc = BankAccount("Alice", 1000)
acc.deposit(500)
acc.withdraw(200)
print(acc)  # Alice 的账户，余额：1300
```

**43.**
```python
class Student:
    def __init__(self, name):
        self.name = name
        self.scores = {}
    
    def add_score(self, subject, score):
        self.scores[subject] = score
    
    def get_average(self):
        if not self.scores:
            return 0
        return sum(self.scores.values()) / len(self.scores)

class StudentManager:
    def __init__(self):
        self.students = []
    
    def add_student(self, student):
        self.students.append(student)
    
    def get_top_student(self):
        if not self.students:
            return None
        return max(self.students, key=lambda s: s.get_average())

# 测试
s1 = Student("Alice")
s1.add_score("Math", 90)
s1.add_score("English", 85)

s2 = Student("Bob")
s2.add_score("Math", 95)
s2.add_score("English", 92)

manager = StudentManager()
manager.add_student(s1)
manager.add_student(s2)

top = manager.get_top_student()
print(f"最高分：{top.name}，平均分：{top.get_average():.1f}")
```

**44.**
```python
class Node:
    def __init__(self, value):
        self.value = value
        self.next = None

class LinkedList:
    def __init__(self):
        self.head = None
    
    def append(self, value):
        new_node = Node(value)
        if self.head is None:
            self.head = new_node
            return
        current = self.head
        while current.next:
            current = current.next
        current.next = new_node
    
    def __iter__(self):
        current = self.head
        while current:
            yield current.value
            current = current.next

ll = LinkedList()
ll.append(1)
ll.append(2)
ll.append(3)
for val in ll:
    print(val)  # 1, 2, 3
```

</details>

---

*练习题整理时间：2026-09-13*

# Python 模块与输入输出 — 练习题

> 基于第6章（模块）+ 第7章（输入与输出）

---

## 一、选择题

### 1. 以下哪种导入方式可以直接使用 `sqrt` 而不需要加模块前缀？B

A. `import math`  
B. `from math import sqrt`  
C. `import math as m`  
D. `import sqrt from math`

---

### 2. 当模块被 `import` 导入时，`__name__` 的值是？B

A. `"__main__"`  
B. 模块文件名  
C. `None`  
D. `"import"`

---

### 3. 以下代码的输出是什么？B

```python
name = "Python"
version = 3.14
print(f"{name} {version:.1f}")
```

A. `Python 3.14`  
B. `Python 3.1`  
C. `Python 3`  
D. 报错

---

### 4. 打开文件进行**追加写入**，应使用的模式是？C

A. `"r"`  
B. `"w"`  
C. `"a"`  
D. `"w+"`

---

### 5. 以下哪种写法能**自动关闭**文件？B

A. `f = open("test.txt"); f.read(); f.close()`  
*B. `with open("test.txt") as f: f.read()`*  
C. `open("test.txt").read()`  
D. B 和 C 都能

---

### 6. 将 Python 字典保存到 JSON 文件，应使用？C

A. `json.dumps()`  
B. `json.loads()`  
C. `json.dump()`  
D. `json.load()`

---

### 7. 以下代码的输出是什么？B

```python
x = 42
print(f"答案：{x:0>5}")
```

A. `答案：42`  
B. `答案：00042`  
C. `答案：    42`  
D. `答案：42000`

---

### 8. 关于 `from module import *`，以下说法正确的是？~~D~~(C)

A. 这是最佳实践  
B. 只导入函数，不导入变量  
*C. 可能覆盖已有同名变量，不推荐*  
D. 等同于 `import module`

---

### 9. 读取文件所有行并返回列表，应使用？~~B~~ C

A. `f.read()`  
B. `f.readline()`  
*C. `f.readlines()`*  
D. `f.readall()`

---

### 10. 以下代码的输出是什么？B

```python
import json
data = [1, "hello", True]
s = json.dumps(data)
print(type(s))
```

A. `<class 'list'>`  
B. `<class 'str'>`  
C. `<class 'dict'>`  
D. `<class 'bytes'>`

---

## 二、填空题

### 11. 给模块起别名的语法是 `import math __as____ m`。

---

### 12. f-string 中，保留2位小数的格式说明符是 `{value:__.2f____}`。

---

### 13. 用 `with` 打开文件的好处是___自动关闭文件___。

---

### 14. 读取二进制文件（如图片）时，open 的 mode 应设为___rb___。

---

### 15. 从 JSON 字符串解析为 Python 对象，使用 `json.__loads____()`。

---

### 16. 包的目录下必须有一个____init_.py___文件。

---

### 17. 当直接运行脚本时，`__name__` 的值是___main___。

---

### 18. 查看模块中定义了哪些名称，使用___dir()___函数。

---

## 三、判断题

### 19. `import math` 和 `from math import sqrt` 导入后，调用 `sqrt()` 的方式相同。（ X）

---

### 20. 用 `"w"` 模式打开已存在的文件，原有内容会被保留。（ X）

---

### 21. `json.dumps()` 把 Python 对象转成字符串，`json.loads()` 把字符串转回 Python 对象。（V ）

---

### 22. f-string 的花括号里可以写任意 Python 表达式。（ V）

---

### 23. 文件用 `with` 打开后，离开 with 块文件会自动关闭。（V ）

---

### 24. `"hello".center(10)` 返回 `'  hello   '`（居中，总宽度10）。（ V）

---

## 四、代码阅读题

### 25. 写出输出：

```python
a = 10
b = 3
print(f"{a} ÷ {b} = {a/b:.2f}")
10 ÷ 3 = 3.33
```

---

### 26. 写出输出：

```python
items = ["apple", "banana", "cherry"]
for i, item in enumerate(items):
    print(f"{i+1}. {item:>10}")

1.     apple
2.    banana
3.    cherry
```

---

### 27. 写出输出：

```python
import json
data = {"name": "Tom", "scores": [90, 85, 92]}
s = json.dumps(data, ensure_ascii=False)
print(s)

{"name": "Tom", "scores": [90, 85, 92]}
```

---

### 28. 文件 `data.txt` 内容为：
```
hello
world
python
```

写出输出：
```python
with open("data.txt", "r", encoding="utf-8") as f:
    lines = f.readlines()
    print(len(lines))
    print(lines[1].strip())
    
 3
world
```

---

## 五、编程题

### 29. 创建一个模块

编写文件 `calculator.py`，包含：
- `add(a, b)` 函数
- `multiply(a, b)` 函数
- 在 `if __name__ == "__main__":` 下写测试代码



def add(a, b):

​	return a+b

def multiple(a, b):

​	return a-b

if __name__ == "__main__":

​	a = add(2,1)

​	b = multiple(2,1)

---

### 30. f-string 格式化

给定数据：
```python
products = [
    ("笔记本电脑", 5999.5),
    ("鼠标", 129.0),
    ("键盘", 299.9),
]
```

用 f-string 输出整齐的表格：
```
商品名称      价格
笔记本电脑    5999.50
鼠标          129.00
键盘          299.90

print(f"{'商品名称':<10}{'价格':>10}")
for name, price in products:
    print(f"{name:<10}{price:>10.2f}")
```

---

### 31. 文件读写

编写代码：
1. 创建文件 `diary.txt`，写入今天的日期和一行日记
2. 再追加一行日记
3. 读取并打印全部内容



---

### 32. JSON 数据持久化

编写一个"通讯录"程序：
1. 定义联系人数据（字典形式）
2. 保存到 `contacts.json`
3. 从文件读取并打印

---

### 33. 统计文件

编写代码：
1. 创建文件 `numbers.txt`，每行写一个数字（1到10）
2. 读取文件，计算所有数字的总和
3. 打印结果

---

### 34. 综合题：简易记账本

编写程序：
1. 用列表存储消费记录：`[{"item": "午餐", "price": 35}, ...]`
2. 保存到 JSON 文件
3. 读取后计算总消费
4. 用 f-string 格式化输出每条记录和总计

---

## 参考答案

<details>
<summary>点击查看答案</summary>

### 选择题

1. B
2. B
3. B
4. C
5. B
6. C
7. B
8. C
9. C
10. B

### 填空题

11. `as`
12. `.2f`
13. 自动关闭文件（即使发生异常）
14. `"rb"`
15. `loads`
16. `__init__.py`
17. `"__main__"`
18. `dir()`

### 判断题

19. ✗（前者要 `math.sqrt()`，后者直接 `sqrt()`）
20. ✗（会被覆盖）
21. ✓
22. ✓
23. ✓
24. ✓

### 代码阅读题

**25.**
```
10 ÷ 3 = 3.33
```

**26.**
```
1.      apple
2.     banana
3.     cherry
```

**27.**
```
{"name": "Tom", "scores": [90, 85, 92]}
```

**28.**
```
3
world
```

### 编程题

**29.**
```python
# calculator.py
def add(a, b):
    return a + b

def multiply(a, b):
    return a * b

if __name__ == "__main__":
    print(f"3 + 5 = {add(3, 5)}")
    print(f"3 × 5 = {multiply(3, 5)}")
```

**30.**
```python
products = [
    ("笔记本电脑", 5999.5),
    ("鼠标", 129.0),
    ("键盘", 299.9),
]

print(f"{'商品名称':<10}{'价格':>10}")
for name, price in products:
    print(f"{name:<10}{price:>10.2f}")
```

**31.**
```python
from datetime import date

# 写入
with open("diary.txt", "w", encoding="utf-8") as f:
    f.write(f"{date.today()} 今天开始学Python\n")

# 追加
with open("diary.txt", "a", encoding="utf-8") as f:
    f.write("学会了文件读写，开心！\n")

# 读取
with open("diary.txt", "r", encoding="utf-8") as f:
    print(f.read())
```

**32.**
```python
import json

contacts = {
    "Alice": {"phone": "13800001111", "email": "alice@example.com"},
    "Bob": {"phone": "13800002222", "email": "bob@example.com"},
}

# 保存
with open("contacts.json", "w", encoding="utf-8") as f:
    json.dump(contacts, f, ensure_ascii=False, indent=2)

# 读取
with open("contacts.json", "r", encoding="utf-8") as f:
    loaded = json.load(f)
    for name, info in loaded.items():
        print(f"{name}: {info['phone']}")
```

**33.**
```python
# 创建文件
with open("numbers.txt", "w", encoding="utf-8") as f:
    for i in range(1, 11):
        f.write(f"{i}\n")

# 读取并求和
total = 0
with open("numbers.txt", "r", encoding="utf-8") as f:
    for line in f:
        total += int(line.strip())

print(f"总和：{total}")  # 55
```

**34.**
```python
import json

# 数据
records = [
    {"item": "午餐", "price": 35},
    {"item": "咖啡", "price": 28},
    {"item": "地铁", "price": 5},
    {"item": "晚餐", "price": 42},
]

# 保存
with open("ledger.json", "w", encoding="utf-8") as f:
    json.dump(records, f, ensure_ascii=False, indent=2)

# 读取并输出
with open("ledger.json", "r", encoding="utf-8") as f:
    data = json.load(f)

print("今日消费：")
total = 0
for r in data:
    print(f"  {r['item']:<6} ¥{r['price']:>6.2f}")
    total += r["price"]

print(f"{'─' * 20}")
print(f"  {'总计':<6} ¥{total:>6.2f}")
```

</details>

---

*练习题整理时间：2026-09-11*
